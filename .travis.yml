language: ruby
rvm:
  - 2.2
  - 2.3
  - 2.4
  - ruby-head
matrix:
  fast_finish: true
  allow_failures:
    - rvm: ruby-head
branches:
  only:
    - "master"
    - "/^release-/"
before_install: gem install bundler
cache: bundler
sudo: false
git:
  depth: 10
